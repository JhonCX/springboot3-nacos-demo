server:
  port: 13000

spring:
  main:
    banner-mode: off
  application:
    name: demo
  profiles:
    active: local

rpc:
  addr: 127.0.0.1:8848

logging:
  file:
    name: logs/${spring.application.name}/all.log